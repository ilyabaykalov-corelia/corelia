# Диагностика 504 — 11 сентября 2026

Оба внешних запроса падают при ожидании BPMU Task List:
`https://element.aplana-it.ru/platformv/api/tasklist/system/v2/tasks:search`.

- GET карточки b00cc3a6-39db-481c-9c9a-97e6d9024146: DataSpace 200 за 300–696 мс; вложения 200 за 486–852 мс; workflow 504 примерно через 30 секунд.
- GET /api/core/v1/tasks/summary: workflow 504 примерно через 30 секунд.
- В 13:08 МСК BPMU кратковременно отвечал за 6,36 с и 120 мс, summary вернул 200 за 6,87 с. В 13:09–13:12 запросы снова завершались по таймауту.
- DNS/TCP/TLS до BPMU доступны из контейнера: запрос без авторизации получил ожидаемый 401 за 159 мс. Это не проверяет исполнение авторизованного поиска.
- Эксперимент с HTTP/1.1 вместо HTTP/2 в workflow повторил 504: 13:15:28 и 13:15:30 МСК. Гипотеза протокола не подтвердилась. Исходный код и исходный образ восстановлены, временный mount JAR убран.
- Формат поиска сравнен с прежними BFF: тот же /system/v2/tasks:search, attributes=*, limit=100, offset=0, scopes EXECUTOR/MANAGER; фильтр документа attributes.documentId={value,exact:true}.

Для продолжения нужны логи сервера BPMU Task List и входного proxy за 13:09–13:16 МСК, включая время обработки поиска, ошибки БД и состояние пула соединений. Это направления диагностики, а не подтверждённая причина.

Примеры requestId Corelia (в текущем ParallelCalls MDC не переносится в поток внешнего вызова):
- bde89b26-403f-465d-be89-115e69f2a851 — карточка, 13:09:48–13:10:19.
- e3bf262f-225e-4e5b-a797-b757f8bff953 — summary, 13:09:48–13:10:18.

Изменения бизнес-данных и стабильных React/Platform V не выполнялись. Повышение таймаута или подмена ошибки пустыми задачами не применялись. Ошибка на реальном стенде остаётся нерешённой.

## Повтор после восстановления контрактов dev-2, 17:06 МСК

Документ `aaff0904-6db9-49c6-9e04-df5917e9c757` создан; загрузка вложения вернула 201.
Для открытия карточки requestId `2a4147e3-df80-4f28-9d8b-09f6e29caf0c`:

- 17:06:02.537–17:06:02.820: document-service 200, 282 мс.
- 17:06:02.821–17:06:03.404: attachment-service 200, 582 мс.
- 17:06:03.406–17:06:33.429: workflow-service 504, 30022 мс.
- BPMU Task List завершился клиентским таймаутом 30005–30006 мс на
  `https://element.aplana-it.ru/platformv/api/tasklist/system/v2/tasks:search`.
- Gateway вернул 504 за 30912 мс. Аналогичный запрос в 17:05:42–17:06:13
  имел requestId `3cf61dbe-f85a-4d1f-8c59-7df3392594a8`.

Путь поиска, параметры `attributes=*`, `limit=100`, `offset=0`, scopes EXECUTOR/MANAGER,
заголовки X-Username/X-Roles и фильтр по documentId сверены с прежним BFF и локальной
спецификацией `bpmu-task-list-system-api-v2-2.0.8.json`. Несовместимость формата поиска
не обнаружена. Это не доказывает исправность исполнения запроса на сервере.

Для установления первопричины нужны access/error-логи входного proxy и BPMU Task List
за 17:05:40–17:06:35 МСК (14:05:40–14:06:35 UTC), а также сведения о зависимостях
Task List. Доступ к этим серверным логам в текущем окружении не установлен.
Код сервисов и стабильные ветки не менялись; ошибка остаётся нерешённой.
